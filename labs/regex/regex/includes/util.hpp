#pragma once
#include <string>

namespace util
{
    std::string find_email(const std::string &);
    std::string find_social(const std::string &);
    // FIXME - add function prototype for find_credit_card function
}